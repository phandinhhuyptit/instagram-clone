import React from 'react';
import {View, Text} from 'react-native';
import RootStackNavigation from '~navigations/index';
// import { SafeAreaProvider } from "react-native-safe-area-context"

const App = () => {
  return <RootStackNavigation />;
};

export default App;
